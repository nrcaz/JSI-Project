{% extends 'base.html.twig' %}

{% block title %}UserAdmin{% endblock %}

{% block body %}
    <h1>UserAdmin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_admin.id }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ user_admin.login }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user_admin.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user_admin.password }}</td>
            </tr>
            <tr>
                <th>PasswordKey</th>
                <td>{{ user_admin.passwordKey }}</td>
            </tr>
            <tr>
                <th>PasswordExpirationKey</th>
                <td>{{ user_admin.passwordExpirationKey ? user_admin.passwordExpirationKey|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_admin_index') }}">back to list</a>

    <a href="{{ path('user_admin_edit', {'id': user_admin.id}) }}">edit</a>

    {{ include('user_admin/_delete_form.html.twig') }}
{% endblock %}
